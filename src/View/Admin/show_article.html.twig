{% extends 'layout.html.twig' %}
{% block title %}Article #{{article.id}}{% endblock %}
{% block content %}
{{dump(article)}}
<div class="container">
    <div class="jumbotron mt-5">
        <div class="row">
            <div class="col-lg-4 col-md-4 col-xs-12">
                <ul class="list-group">
                    {% for img in article.images %}
                    <li class="list-group-item"><img src="{{img.url }}" class="card-img-top"
                            alt="air max 270"></li>
                    {% endfor %}
                </ul>
            </div>
            <div class="col-lg-8 col-md-8 col-xs-12">
                <div class="title">
                    <h5 class="card-title">{{article.model}}</h5>
                </div>
                <div class="col-12">
                    <form method="POST">
                        <p>{{article.price}} $</p>
                        <input type="text" class="d-none" name="add_article" value="{{article.id}}">
                        <button type="submit" class="btn btn-primary"><i class="fas fa-cart-arrow-down"></i></a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}